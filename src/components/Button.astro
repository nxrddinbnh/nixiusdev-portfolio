---
const { link, className } = Astro.props;
---

<a href={link} target="_blank" class={`py-3 text-white font-semibold rounded-xl mr-4 ${className} transition-transform duration-300 transform-gpu shadow-md hover:shadow-lg hover:-translate-y-1`}>
    <slot />
</a>